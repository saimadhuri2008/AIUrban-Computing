# Sector Redesign Mastermap — Output

This folder contains the interactive HTML generated by `build_masterdesign_enhanced_v3.py`.

To reproduce locally:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python scripts/redesign/build_masterdesign_enhanced_v3.py --help
```

Inputs used (see provenance.json for full details).
